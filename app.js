/* Конфиг */
var config = require('./config');
/* NameSpace модуля trade */
var SteamTradeNameSpace = require('steam-tradeoffers');
/* Новый объект трейда */
var trade = new SteamTradeNameSpace();

/* */
trade.setup({
	sessionID: '',
	webCookie: '',
	APIKey: 
});